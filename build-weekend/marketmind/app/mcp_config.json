{
  "mcpServers": {
    "reddit-scraper": {
      "command": "npx",
      "args": [
        "-y",
        "@apify/actors-mcp-server@latest",
        "--actors",
        "labrat011/reddit-scraper"
      ],
      "env": {
        "APIFY_TOKEN": "${APFY_TOKEN}"
      },
      "description": "On-demand qualitative intel: defect traps, long-tail collector valuation, and secondhand trend discovery via labrat011/reddit-scraper"
    },
    "n8n-template-scraper": {
      "command": "npx",
      "args": [
        "-y",
        "@apify/actors-mcp-server@latest",
        "--actors",
        "datavoyantlab/n8n-templates-scraper"
      ],
      "env": {
        "APIFY_TOKEN": "${APFY_TOKEN}"
      },
      "description": "On-demand n8n community workflow template discovery, node benchmarking, and raw JSON extraction via datavoyantlab/n8n-templates-scraper"
    }
  }
}
