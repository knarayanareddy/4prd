# Sitting 9 — Same jury, four product specs (not four skins)

Sitting 8 scored the live harness. This sitting scores the four SDD trees as they sit on disk: `listguard/`, `clausewindow/`, `menumind/`, `exhibit/`.

They will not let us implement four skins. They will not pick Monday’s human. They scored **whether each spec would survive 16:00 if that door is the one we walk through**.

---

## Jury (by product)

### ListGuard

**Priya:** “Accept says it writes `actor=human`. Sitting 8 found that was RAM. The AC must say *stored receipt*. Tasks T5–T11 still build a harness that already exists. Seed ids are named and not in `evals/listguard/`.”

**Jonas:** “Override is missing from US-1. Hostile id is `lg-inject-01`. If T12 forgets `HOSTILE_FIXTURE`, the one-click is still the stub watch.”

**Elena:** “The seed list is a promise. The files are not in the repo.”

**Marta:** “Plan stores uploads in `/tmp`. Harness law is `uploads/`.”

**Anika:** “Still the default if nobody replies. Do not build it tonight.”

### ClauseWindow

**Jonas:** “I can hold `redlines.json`. Hostile is `cw-inject-01`. Good. Tasks put the named lawyer in the *footer*. MASTER put them in the chrome.”

**Priya:** “Phase 0 is copied from ListGuard as if `app/` were empty.”

**Markus:** “Not-legal-advice banner stays. Do not add ‘you should sign’.”

**Kenji:** “1M may hang. Cuts already exist. Do not silently chunk policy.”

### MenuMind

**Markus:** “Article V says open hostile. Satay is physical harm. Keep satay as the opener; inject is card 2 and in gold. Write the exception so an agent does not ‘fix’ it.”

**Camille:** “Plan says red `unknown` chips. MASTER says yellow `#C5A202` + ink + the word UNKNOWN. That is a fail.”

**Anika:** “First 15s is still the pitch risk. Spec already says platforms, not OCR. Keep it.”

**Priya:** “Tasks rebuild the harness. Don’t.”

### Exhibit

**Markus:** “No Compliant button. Pass. HTML pack preview as P0 is how ‘compliance platform’ sneaks back in.”

**Jonas:** “Sitting 8 already cut the HTML view. JSON is the artifact I hold.”

**Dima:** “TF evals. Heuristic must stay labelled. The schema example hard-codes `disclosure_present: true` — that reads as a claim.”

**Leila / Kenji:** “T13 spans and T15 JSON are already in the harness. A Tuesday agent that follows tasks.md will rebuild them and ship a second skin by accident.”

**Sofia:** “Judge-human agreement still `not measured`. Keep that sentence.”

---

## Council vote — spec tweaks only. Do not ship four skins.

| # | Tweak | Product |
|---|---|---|
| 1 | Phase 0 tasks = done in `app/`. Tuesday starts T12 | all four |
| 2 | Accept persists stored receipt; Override queues + `human_override` | all four |
| 3 | Name `HOSTILE_FIXTURE` id in spec + unimplemented package | all four |
| 4 | Pack layer `GET /exhibit/{id}` available whichever skin | all four |
| 5 | Seed gold for the *named* ids (not n=40) | all four |
| 6 | ListGuard plan: `uploads/` not `/tmp` | ListGuard |
| 7 | ClauseWindow: named human in chrome, not footer | ClauseWindow |
| 8 | MenuMind: UNKNOWN yellow+ink; Article V exception for satay opener | MenuMind |
| 9 | Exhibit: HTML view P1; tasks T13/T15 JSON already done | Exhibit |

**Rejected:** four skins, n≥40 tonight, HTML exhibit as P0, picking Monday’s product, fake ALLOW, red allergen chips, rebuilding the desk.

Monday 18:00 table is unchanged. Default ListGuard. Pack is a layer.

Implemented in this sitting in the four SDD trees + seed gold + package ids.
